.env
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
logs/
*.csv
!data/generate_data.py
.DS_Store
*.log

# ML artefacts — can be large, regenerated by training
catboost_info/
lightning_logs/
*.ckpt
*.pkl
*.pt
*.pth
# Root-level ML model-weight binaries only — anchored so it does NOT swallow
# dbt/models/ (the SQL transformation models, which must be version-controlled).
/models/

# Locally-built dbt+DuckDB mart artifacts (regenerable via db/build_marts.py)
*.duckdb
data/parquet/

# One-off utility scripts not part of the app
replace_css.py

# Windows null device artefact
nul

# Ruff cache
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*~
