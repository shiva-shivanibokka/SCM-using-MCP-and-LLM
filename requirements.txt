# ============================================================
# Petopia Intelligence Hub — root tooling only
# ============================================================
# The forecasting + API backend dependencies live in backend/requirements.txt
# (FastAPI, chronos-forecasting, neuralforecast, catboost, torch, LLM SDKs).
# The frontend dependencies live in frontend/package.json.
#
# This file retains only what data generation + the test suite need:

numpy>=1.26.0,<2.0
pandas>=2.1.0
pytest>=8.0.0
