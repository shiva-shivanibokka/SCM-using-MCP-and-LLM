# Developer / data-engineering tooling — NOT needed by the deployed backend.
# The HF Space runtime never runs dbt; transformations run here or in CI.
#
#   pip install -r requirements-dev.txt
#
# dbt transforms the raw seeded tables into the analytics marts the app reads.
dbt-core>=1.8,<2.0
dbt-postgres>=1.8,<2.0

# Free-tier / no-database path: build the same marts locally with dbt + DuckDB
# over a Parquet raw layer. See db/build_marts.py and db/export_parquet.py.
dbt-duckdb>=1.8,<2.0
duckdb>=1.0.0
pyarrow>=15.0.0
