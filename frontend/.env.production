# Baked into the production build by Vite (`npm run build`).
# Public backend URL (HF Space) — not a secret. The Vercel dashboard env var
# VITE_API_BASE, if set, overrides this.
VITE_API_BASE=https://shiva-1993-petstore-scm-mcp-backend.hf.space
